import { Slide } from '@mui/material';
import React from 'react';

function Payements(props) {
    return (
       <Slide direction="right" in={true} timeout={500}>
            
        </Slide>
    );
}

export default Payements;